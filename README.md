# Rails api server following the JSONAPI spec

After cloning the project:

* run `./bin/setup`
* modify your **config/database.yml** file
* (optinal) add dummy data via `rake db:seed`
* start the app via `foreman start`
